{
    "name": "My New Task",
    "module_type": "template",
    "enabled": true,
    "schedule": {
        "trigger": "interval",
        "seconds": 60
    },
    "_schedule_comment": "Defines the task's execution schedule. Use 'interval' for frequent tasks (in seconds) or 'cron' for traditional scheduling (e.g., '*/5 * * * *' for every 5 minutes).",
    "debug": false,
    "_debug_comment": "Enable debug mode to get more verbose logs from this task.",
    "settings": {
        "example_setting": "Hello, World!",
        "debug_message": "This is a sample debug message."
    },
    "_settings_comment": "Use this section to define any custom parameters your task needs. These are accessed via the 'config['settings']' dictionary in your Python script."
}
